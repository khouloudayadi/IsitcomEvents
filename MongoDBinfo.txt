* l'information est modélisée sur un document au format JSON.
* mongod : lancer le moteur de base de données :le port 27017 par défaut utilisé par MongoDb.
* mongo : lancer l'intrepréteur de commandes 
* créer une base de données : use projet
* tables = collections (Une collection est donc un ensemble de documents de même nature)
* creation de collection :db.createCollection('user')
* insertion(La création de la collection est implicite):  db.<nom de la collection>.insert( <document> )
    expl : db.acteurs.insert({nom:"Johansson", prenom:"Scarlett"})
* pour vérifier que notre donnée est bien insérée:db.acteurs.find()

* show dbs
* show collections